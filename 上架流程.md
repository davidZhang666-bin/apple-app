# KYHSApp 上架流程

App Store 上架五步，外加第一步详细配置。

---

## 五步总览

1. **Xcode 工程配置** — 签名、版本号、entitlements
2. **App Store Connect 建 App** — 注册 Bundle ID、填基本信息
3. **Archive 上传** — Xcode 打包并上传到 App Store Connect
4. **填元数据** — 截图、描述、隐私政策、演示账号
5. **提交审核** — 备注 HTTP 视频流来源，等审核结果

提审前建议先丢 TestFlight 真机走一遍主流程。

---

## 第一步：Xcode 工程配置（详细）

### 1.1 打开工程并选中 Target

```bash
open KYHSApp/KYHSApp.xcodeproj
```

- 左侧 Project Navigator（`⌘1`）→ 点最顶部蓝色 **KYHSApp** 图标
- 中间面板会出现 PROJECT / TARGETS 两栏 → 选 **TARGETS → KYHSApp**

### 1.2 General 标签页

**Supported Destinations**：只保留 iPhone，删掉 iPad / Mac / Apple Vision（保留会被审核员在 iPad 上测，UI 大概率挂）。

**Minimum Deployments**：建议从 18.0 改成 **16.0** 或 **17.0**，覆盖更多用户。不改也行。

**Identity**：

| 字段 | 当前 | 怎么填 |
|---|---|---|
| Display Name | 康源华善 | 不动 |
| Bundle Identifier | cn.kangyuanhuashan.app | 不动 |
| Version | 1.0.0 | 首次 1.0.0，之后递增 |
| Build | 1 | **每次上传必须 +1**，被打回重传也要加 |

### 1.3 Signing & Capabilities 标签页

切到这个标签页，看 **Release** 那栏：

| 字段 | 怎么填 |
|---|---|
| Automatically manage signing | 勾上 |
| **Team** | 下拉选你的开发者账号（**必填**）。⚠️ **不能选 "Personal Team"**，那是免费账号，不能上架。要选付费的 Apple Developer Program 账号（一般显示为公司名或个人真名，不带 Personal Team 字样） |
| Bundle Identifier | cn.kangyuanhuashan.app |
| Provisioning Profile | 选完 Team 自动生成 "Xcode Managed Profile" |
| Signing Certificate | 自动显示 "Apple Distribution" |

**Team 下拉里没账号？**
Xcode → Settings → Accounts → 左下 ＋ → Apple ID → 用开发者账号登录。

**报 "Communication with Apple failed / No profiles found"？**
那是 Development Profile 的报错（要求绑定真机），**不影响上架**，忽略即可。Archive 走的是 Distribution，跟这个无关。
要消除警告：iPhone 连 Mac → Window → Devices and Simulators → 选设备 → 回 Signing 页点 Try Again。

### 1.4 处理 entitlements

`KYHSApp.entitlements` 现在内容是 `aps-environment = development`，但代码里没用推送，**直接移除最干净**：

1. **Build Settings** 标签页 → 搜 `entitlements`
2. **Code Signing Entitlements** 行，Debug 和 Release 两栏的值都删空
3. 文件树右键 `KYHSApp.entitlements` → Delete → **Move to Trash**
4. 回 Signing & Capabilities，若还有 "Push Notifications" capability 点右上角 × 删掉

未来加推送再加回来，那时值要写 `production`。

### 1.5 Build Settings 关键项核对

Build Settings → All + Combined，搜确认：

| Key | Release 值 |
|---|---|
| CODE_SIGN_STYLE | Automatic |
| DEVELOPMENT_TEAM | 你的 Team ID |
| PRODUCT_BUNDLE_IDENTIFIER | cn.kangyuanhuashan.app |
| SWIFT_VERSION | 5.0 |

### 1.6 Scheme 配成 Release

`⌘<` 打开 Edit Scheme → 左栏选 **Archive** → 右边 Build Configuration 确认是 **Release**（默认就是）。

### 1.7 编译验证

```bash
xcodebuild -project KYHSApp/KYHSApp.xcodeproj -scheme KYHSApp \
  -configuration Release \
  -destination 'generic/platform=iOS' \
  clean build
```

第一次会让你输 Mac 钥匙串密码（让 codesign 访问私钥），勾"始终允许"。

---

## 第二步：App Store Connect 建 App

进 [appstoreconnect.apple.com](https://appstoreconnect.apple.com) → My Apps → ＋ → New App：

- Platform：iOS
- Name：康源华善
- Primary Language：简体中文
- Bundle ID：cn.kangyuanhuashan.app
- SKU：自定义唯一字符串（如 `kyhs-app-001`）
- User Access：Full Access

---

## 第三步：Archive 上传

Xcode 顶部 device 选 **Any iOS Device (arm64)**（不能是模拟器）→ Product → **Archive**

Archive 完成后 Organizer 自动弹出：
**Distribute App** → **App Store Connect** → **Upload** → 自动管理签名 → Upload

等 10–30 分钟，App Store Connect → TestFlight 里能看到 build 即上传成功。

---

## 第四步：填元数据

App Store Connect → 你的 App → App Store 标签页：

| 项目 | 要求 |
|---|---|
| 名称 | 康源华善（≤30 字符） |
| 副标题 | ≤30 字符 |
| 关键词 | ≤100 字符，逗号分隔 |
| 描述 | ≤4000 字符 |
| **截图** | **6.9" 必传**（1320×2868），3–10 张 |
| 应用图标 | 1024×1024（已有） |
| 隐私政策 URL | **必填** |
| 分类 | 商务 / 工具 |
| 年龄分级 | 自评问卷 |
| **演示账号** | **必填**，店长测试号，不填秒拒 |

---

## 第五步：提交审核

在 App Review Information → Notes 里备注：

> 视频流来自第三方 CDN，暂不支持 HTTPS，因此 Info.plist 开启了 NSAllowsArbitraryLoads。

填完点 **Submit for Review**，中国区 24–72 小时出结果。

被拒就在 Resolution Center 回复或重传 build（记得 Build 号 +1）。
